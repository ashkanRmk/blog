---
interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
  {
    date.toLocaleDateString("fa-IR-u-ca-persian", {
      month: "short",
      day: "numeric",
      year: "numeric",
    })
  }
</time>
